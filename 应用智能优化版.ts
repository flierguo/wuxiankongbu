/**
 * 智能优化版应用入口
 * 
 * 本文件用于启动和应用智能优化系统
 * 只需在 Main.ts 中导入并调用即可
 */

// 导入智能优化版的所有功能
import {
    怪物掉落物品触发,
    计算伤害,
    装备属性统计,
    快速启动智能优化,
    打印性能报告,
    获取所有性能统计,
    清空所有缓存,
    重置所有统计
} from './性能优化/index_智能优化';

/**
 * 启动智能优化系统
 * 在服务器启动时调用一次
 */
export function 启动智能优化系统(): void {
    快速启动智能优化();
    

}

/**
 * 导出优化后的函数，供其他模块使用
 */
export {
    怪物掉落物品触发,
    计算伤害,
    装备属性统计,
    打印性能报告,
    获取所有性能统计,
    清空所有缓存,
    重置所有统计
};

/**
 * 默认导出（便于统一导入）
 */
export default {
    // 启动函数
    启动: 启动智能优化系统,
    
    // 核心优化函数
    怪物掉落物品触发,
    计算伤害,
    装备属性统计,
    
    // 监控和管理
    打印性能报告,
    获取所有性能统计,
    清空所有缓存,
    重置所有统计
};

/**
 * 使用说明
 * 
 * 1. 在 Main.ts 中添加：
 *    import { 启动智能优化系统 } from './应用智能优化版';
 *    启动智能优化系统();
 * 
 * 2. 在需要使用的地方导入：
 *    import { 怪物掉落物品触发, 计算伤害, 装备属性统计 } from './应用智能优化版';
 * 
 * 3. 使用方式与原版本完全相同：
 *    怪物掉落物品触发(player, monster, item);
 *    计算伤害(attacker, target, skillId, damageMultiplier);
 *    装备属性统计(player, onItem, offItem, position);
 * 
 * 4. 查看性能统计（可选）：
 *    import { 打印性能报告 } from './应用智能优化版';
 *    打印性能报告();
 * 
 * 注意：
 * - 游戏引擎不支持 setInterval/setTimeout
 * - 性能报告需要手动调用或集成到 RobotManageNpc 定时器中
 * - 所有核心优化功能均正常工作
 */

